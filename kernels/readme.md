# Kernel usage

There are different platform to run PTX in rust.
We show with cudarc and cuda-oxide

## cudarc 
It can import the ptx then run it

## cuda-oxide
It provide the whole toolchain, including compile the rust code to PTX.

